require('dotenv').config(); const express = require('express'); const cors = require('cors'); const path = require('path'); const productsRouter = require('./routes/products'); const miscRouter = require('./routes/misc'); const trendsRouter = require('./routes/trends'); const campaignsRouter = require('./routes/campaigns'); const ordersReviewsRouter = require('./routes/orders'); const analyticsRouter = require('./routes/analytics'); const rankingRouter = require('./routes/ranking'); const simulationRouter = require('./routes/simulation'); const { iniciarLoopDeSimulacao } = require('./simulation/engine'); const { inicializarBanco } = require('./db/init'); const app = express(); const PORT = process.env.PORT || 3000; app.use(cors()); app.use(express.json()); app.use(express.static(path.join(__dirname, 'public'))); app.use('/api/products', productsRouter); app.use('/api/trends', trendsRouter); app.use('/api/campaigns', campaignsRouter); app.use('/api/analytics', analyticsRouter); app.use('/api/ranking', rankingRouter); app.use('/api/simulation', simulationRouter); app.use('/api', ordersReviewsRouter); app.use('/api', miscRouter); app.get('/health', (req, res) => res.json({ ok: true })); async function iniciar() { try { await inicializarBanco(); } catch (err) { console.error('Falha ao inicializar o banco automaticamente:', err.message); } app.listen(PORT, () => { console.log(`Mercapp rodando em http://localhost:${PORT}`); const intervalo = Number(process.env.SIMULATION_TICK_MS) || 20000; iniciarLoopDeSimulacao(intervalo); }); } iniciar();